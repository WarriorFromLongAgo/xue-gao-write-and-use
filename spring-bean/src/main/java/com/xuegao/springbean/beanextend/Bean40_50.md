
```
在创建这个对象的时候
org.springframework.boot.web.server.ErrorPageRegistrarBeanPostProcessor
会走到web服务
org.springframework.boot.web.servlet.context.ServletWebServerApplicationContext.onRefresh
org.springframework.boot.web.servlet.context.ServletWebServerApplicationContext.createWebServer

org.springframework.beans.factory.support.DefaultListableBeanFactory.doGetBeanNamesForType
遍历所有的对象的时候
for (String beanName : this.beanDefinitionNames) {
}
先遍历，Bean40_SmartInstantiationAwareBeanPostProcessor
再遍历，Bean50_InstantiationAwareBeanPostProcessor

org.springframework.beans.factory.support.AbstractAutowireCapableBeanFactory.predictBeanType

if (bp instanceof SmartInstantiationAwareBeanPostProcessor) {
    SmartInstantiationAwareBeanPostProcessor ibp = (SmartInstantiationAwareBeanPostProcessor) bp;
    Class<?> predicted = ibp.predictBeanType(targetType, beanName);
    if (predicted != null &&
            (!matchingOnlyFactoryBean || FactoryBean.class.isAssignableFrom(predicted))) {
        return predicted;
    }
}


```